# Rust
/target
**/*.rs.bk

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.iml

# Logs / scratch
*.log
.scratch/

# photohelper runtime caches
.photohelper/

# eng-protocol session-config cache (per-session; not for source control)
.eng-protocol/

# Serena MCP per-machine project state (LSP indexes, on-boarding cache, etc.)
.serena/

# NIMA conversion venv (reproducible via scripts/convert-nima-to-onnx.sh)
scripts/.nima-convert-venv/

# Source Keras weights (not committed; ONNX is the authoritative artifact via LFS)
crates/photohelper-ai/models/*.hdf5
