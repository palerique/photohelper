[package]
name = "photohelper-sidecar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "XMP sidecar I/O (Lightroom-compatible crs: + photohelper ph: namespace)"

[lints]
workspace = true

[dependencies]
# `photohelper-core` re-added when the sidecar reads/writes XMP for
# `Photo` types in session 04+; dropped in D4 to satisfy the new
# `unused_crate_dependencies` workspace lint.
