[package]
name = "photohelper-ai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "AI inference (ONNX Runtime / tract) for culling + improvement"

[lints]
workspace = true

[features]
default = []
# Compile-time gates land alongside the inference wiring in later sessions:
#   ai-culling      = ["dep:ort"]    # NIMA / ARNIQA / face-mesh / MobileCLIP
#   ai-denoise      = ["dep:ort"]    # SCUNet
#   ai-sharpen      = ["dep:ort"]    # Real-ESRGAN
#   tract-fallback  = ["dep:tract"]  # pure-Rust runtime

[dependencies]
# `photohelper-core` re-added when the AI crate consumes `Photo` / `RawImage`
# in session 03+; dropped in D4 to satisfy the new
# `unused_crate_dependencies` workspace lint.
