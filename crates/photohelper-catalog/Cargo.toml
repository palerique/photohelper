[package]
name = "photohelper-catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "SQLite-backed catalog persistence with file-lock + WAL"

[lints]
workspace = true

[dependencies]
photohelper-core = { path = "../photohelper-core" }
rusqlite.workspace = true
fs4.workspace = true
tracing.workspace = true
# `thiserror` + `time` dropped in session 02 D4 — neither is consumed
# by photohelper-catalog today (the catalog's error variants live on
# `photohelper-core::Error`, and Unix-seconds work uses `std::time`).
# Re-add when the catalog acquires its own derive(Error) or `time`-crate
# parsing surface.

[dev-dependencies]
photohelper-test-helpers = { path = "../photohelper-test-helpers" }
tempfile.workspace = true
static_assertions.workspace = true
